import 'package:flutter/material.dart';

class Waitlist extends StatelessWidget {
  const Waitlist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('waitlist'),),
    );
  }
}
